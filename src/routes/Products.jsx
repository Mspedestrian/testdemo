import React, { Component, PropTypes } from 'react';

import ProductList from '../components/Managers/ProductList';
// 引入对应的样式
// 可以暂时新建一个空的

function Products() {

   const showProductProps={
    
  };

  return (
    <div>
      <ProductList />
    </div>
  );
}

export default Products;